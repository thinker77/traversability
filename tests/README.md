Run the dataflow YAML validation tests

1. Create a virtual environment (recommended):

```bash
python -m venv .venv
.
```

2. Install dev requirements:

```bash
pip install -r tests/requirements-dev.txt
```

3. Run tests:

```bash
pytest -q
```
