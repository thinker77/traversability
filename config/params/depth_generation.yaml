depth_generation_node:
  ros__parameters:
    # Densification method: "nearest" | "ip_basic" | "none"
    densification_method: "ip_basic"

    # Max valid depth (beyond this → invalid)
    max_depth_m: 80.0

    # Output encoding: "32FC1" (meters) | "16UC1" (mm)
    output_encoding: "32FC1"
